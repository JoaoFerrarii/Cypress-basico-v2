 ///  <references types="cypress"/>
describe('mapa de renovação',()=>{
    // beforeEach(()=>{
    //     const usuario = Cypress.env("usuarioAcesso")
    //     const senha =  Cypress.env("usuarioAcesso")
    //     cy.login(usuario,senha)
    // })
        // cy.wait(10000)
        // cy.visit('')
        // cy.get('').type('')
    
})
